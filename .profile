# Macports path
export PATH=/opt/local/bin:/opt/local/sbin:/usr/local/bin:$HOME/bin:$PATH

source ~/.bashrc

# All upcoming calendar events appear in new terminal windows
calendar -f /usr/share/calendar/calendar.usholiday -A 7
